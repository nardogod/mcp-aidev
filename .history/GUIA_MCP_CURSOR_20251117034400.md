# Guia de Configuração MCP no Cursor

## ✅ Configuração Concluída

O servidor MCP foi configurado com sucesso no Cursor IDE!

### 📍 Localização da Configuração

A configuração foi salva em:
```
C:\Users\Desktop\AppData\Roaming\Cursor\User\globalStorage\mcp.json
```

### 🔧 Configuração Aplicada

```json
{
  "mcpServers": {
    "mcp-aidev": {
      "command": "python",
      "args": ["-m", "mcp_client.server"],
      "cwd": "C:/LMM-proj/proj_mcp-aidev",
      "env": {
        "GROQ_API_KEY": "your-key-here",
        "MCP_SERVER_URL": "https://mcp-aidev.onrender.com",
        "LLM_PROVIDER": "groq",
        "PYTHONPATH": "C:/LMM-proj/proj_mcp-aidev"
      }
    }
  }
}
```

## 🚀 Próximos Passos

### 1. Configurar Variáveis de Ambiente

**IMPORTANTE:** Você precisa configurar suas chaves de API antes de usar:

#### Opção A: Editar o arquivo de configuração diretamente
1. Execute: `python configurar_mcp_cursor.py --show`
2. Copie o caminho do arquivo
3. Abra o arquivo no editor de texto
4. Substitua `"your-key-here"` pela sua chave GROQ_API_KEY real

#### Opção B: Usar variáveis de ambiente do sistema
Configure as seguintes variáveis de ambiente no Windows:
- `GROQ_API_KEY` - Sua chave da API Groq
- `MCP_SERVER_URL` - URL do servidor MCP (padrão: https://mcp-aidev.onrender.com)
- `LLM_PROVIDER` - Provedor LLM (groq, anthropic, ollama)

Depois execute novamente: `python configurar_mcp_cursor.py`

### 2. Reiniciar o Cursor

**CRÍTICO:** Feche completamente o Cursor IDE e abra novamente para que as mudanças tenham efeito.

### 3. Verificar se Funcionou

Após reiniciar o Cursor:

1. **Verificar logs do MCP:**
   - Abra a paleta de comandos (Ctrl+Shift+P)
   - Procure por "MCP" ou "Model Context Protocol"
   - Verifique se há erros nos logs

2. **Testar as ferramentas:**
   - As ferramentas MCP devem estar disponíveis no Cursor
   - Procure por ferramentas como:
     - `run_agent` - Planejar fases do projeto
     - `get_phase` - Buscar especificação de fase
     - `list_projects` - Listar projetos
     - `update_progress` - Atualizar progresso

3. **Verificar no terminal:**
   - O Cursor deve iniciar automaticamente o servidor MCP
   - Você pode ver mensagens como "MCP-AIDev server starting..." nos logs

## 🛠️ Ferramentas Disponíveis

O servidor MCP expõe as seguintes ferramentas:

### 1. `run_agent`
Executa o LangGraph Agent para planejar fases do projeto automaticamente.

**Parâmetros:**
- `project_name` (string): Nome do projeto
- `project_description` (string): Descrição do projeto
- `max_phases` (int, opcional): Número máximo de fases (padrão: 3)

### 2. `get_phase`
Busca especificação de uma fase do servidor MCP.

**Parâmetros:**
- `project_id` (string): UUID do projeto
- `phase_number` (int): Número da fase

### 3. `list_projects`
Lista todos os projetos cadastrados.

### 4. `update_progress`
Atualiza o progresso de uma fase após implementação.

**Parâmetros:**
- `project_id` (string): UUID do projeto
- `phase_number` (int): Número da fase
- `status` (string): Status da fase (ex: "completed", "in_progress")
- `progress_data` (object, opcional): Dados adicionais de progresso

### 5. `health_check`
Verifica a saúde do servidor MCP.

## 🐛 Solução de Problemas

### Problema: Servidor MCP não inicia

**Solução:**
1. Verifique se o Python está no PATH do sistema
2. Teste manualmente: `python -m mcp_client.server`
3. Verifique os logs do Cursor para erros específicos

### Problema: "Module not found: mcp_client"

**Solução:**
1. Certifique-se de estar no diretório correto do projeto
2. Verifique se todas as dependências estão instaladas: `pip install -r requirements.txt`
3. Verifique se o PYTHONPATH está configurado corretamente

### Problema: "API Key inválida"

**Solução:**
1. Configure a variável `GROQ_API_KEY` corretamente
2. Verifique se a chave está ativa e tem créditos disponíveis
3. Teste a chave manualmente com uma chamada à API Groq

### Problema: Não consigo ver as ferramentas MCP

**Solução:**
1. Reinicie o Cursor completamente (feche todas as janelas)
2. Verifique se o arquivo `mcp.json` está no local correto
3. Verifique os logs do Cursor para erros
4. Tente executar: `python configurar_mcp_cursor.py` novamente

## 📝 Scripts Úteis

### Ver configuração atual
```bash
python configurar_mcp_cursor.py --show
```

### Reconfigurar servidor MCP
```bash
python configurar_mcp_cursor.py
```

### Testar servidor MCP manualmente
```bash
python -m mcp_client.server
```

### Listar projetos no banco
```bash
python list_projects_script.py
```

### Buscar uma fase específica
```bash
python get_phase_script.py
```

## 📚 Documentação Adicional

- [Documentação da API](docs/API.md)
- [README do Projeto](README.md)
- [Especificação MCP](https://modelcontextprotocol.io/)

## 💡 Dicas

1. **Mantenha o servidor MCP atualizado:** Execute `pip install -r requirements.txt` regularmente
2. **Use variáveis de ambiente:** Configure as chaves de API como variáveis de ambiente do sistema para maior segurança
3. **Monitore os logs:** Os logs do Cursor podem ajudar a identificar problemas rapidamente
4. **Teste antes de usar:** Use os scripts de teste para verificar se tudo está funcionando

---

**Status:** ✅ Configurado e pronto para uso (após configurar as chaves de API)

